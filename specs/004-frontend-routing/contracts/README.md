# API Contracts

This feature (Frontend Routing) does not introduce new Backend API contracts. 
It utilizes existing `wailsjs` bindings.

Future routing features that might require backend interaction (e.g., opening a file from OS associating with a deep link) will be documented here.
